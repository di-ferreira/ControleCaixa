unit Manager.Types;

interface

type
  tpFilterOperator = (Eq, GreaterThan, GreaterOrEqual, LessThan, LessOrEqual,
    Like, Different, Between);

  tpOrderDirection = (Asc, Desc);

implementation

end.
